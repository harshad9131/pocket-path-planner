import type { Config } from "tailwindcss";

// Note: We're keeping this file for now, but it won't be used
// since we're moving away from Tailwind CSS
export default {
  content: [],
  theme: {
    extend: {},
  },
  plugins: [],
} satisfies Config;
